# pm-pending-ledger — the ONE canonical pm pending-work SSoT

> This file is **the ONE canonical pm pending-work SSoT**. Before any pm backlog session read
> **THIS file + memory `pm-current-state`**. Do **NOT** re-sweep `_workspace` dirs (consolidated
> here 2026-06-12 then deleted). Counts/SHAs/versions are **POINTERS** — verify live
> (`plugins/palantir-mini/.claude-plugin/plugin.json`, memory `pm-current-state`, `git`).

---

## STATE (pointer, not pin)

- **plugin version** → `plugins/palantir-mini/.claude-plugin/plugin.json` (was **v7.9.0** @ 2026-06-12)
- **backlog W1–W10** → **COMPLETE** (memory `pm-current-state`, 2026-06-11)
- **suite baseline** → **0-fail** (verify live; `bun test` from plugin root)
- **Codex runtime lane** → **HOLD** ("일단"; user directive 2026-06-10) — twins/tests untouchable
- **understand-heart (9-axis FDE → SIC)** → **SACRED**, never reduce

---

## OPEN  (live, actionable now — Claude + pm lane only; all LOW priority)

| id | title | summary | lane | acceptance | source |
|----|-------|---------|------|------------|--------|
| P1 | twin list-ish fields typed `string` | several self-ontology twins declare list-ish fields as `string`; **dataType normalization** is a twin SOURCE edit. Low priority. | pm-off | twin source fields normalized off `string` for list-ish props; registration test green | pm-current-state §Follow-up (a); tracking-dedup-plan §A-OPEN |
| P2 | doc-deprecation note on legacy forwardProp/backwardProp | once **uq-persist (SQLite lane) is formally killed or built**, add the deprecation note; the SQLite writer is `lib/impact-graph/sqlite-cache.deprecated.ts` (do not import live). | pm-off | deprecation note added; gated on uq-persist kill/build decision | pm-current-state §Follow-up (c); W1 settled-as-option-b |
| P3 | agent seeds declare `model:"sonnet"` vs opus-only policy | `seeds/agent-definitions.ts` seeds `implementer/hook-builder/plugin-maintainer/protocol-designer/project-implementer` as `model:"sonnet"`, but harness policy (MEMORY `subagent-model-policy-opus-only`) is **opus-only**. A policy-vs-seed **drift**, modernize candidate. **Deferred from B10-8** (not a removed-surface; do NOT bundle into a deletion PR). | pm-off | seed model fields aligned to opus-only policy; self-check advisory clean | b10-8 proposal SURFACE 2 / Agents |
| P4 | stale MANIFEST.json hash for `src/generated/rule-registry.ts` | trivial cosmetic — stale generated-manifest hash; ungated. | pm-off | MANIFEST.json hash re-synced for the file | tracking-dedup-plan §A (trivial/cosmetic) |

> NOTE: P1–P4 are all LOW. If the user considers any not-worth-tracking, that row may be
> dropped. Surface at do-time.

---

## GATED  (real work, blocked on a named condition)

| id | title | gate (the blocker) | unblocks-when | source |
|----|-------|--------------------|---------------|--------|
| G-CODEX-TWIN | Codex adapter twin build / parity | §0 **Codex HOLD** (user 2026-06-10) — Codex twins/tests untouchable | user lifts the Codex HOLD | backlog-completion §RESOLVED/DROPPED; altitude2 §4. **NOTE:** the 4 Codex Prompt-to-DTC e2e tests + golden CODEX-PARITY-GAP were **DELETED 2026-06-12** per user, so "un-skip" is no longer tracked. |
| G-B10-8-DELETE | ~13 removed-tool MCP handler `.ts` files + their `self/functions.ts` `FUNCTION_INSTANCES` seed rows + tests (set **B** in b10-8 proposal) | **DELETION_READINESS** — the whole plugin tree is a pm PROTECTED_SURFACE; the gate refuses all physical deletion while Codex runtime evidence is source-only | a **live-runtime** Codex reinstall/reload/restart smoke ref (non `tests/`) is recorded | b10-8 proposal SURFACE 1 / set B |
| G-A5 | `sqlite-cache.deprecated` → convex swap | **BLOCKED** — `convex-client` is stub-only (returns empty) | re-scope: **(a)** keep SQLite + rename off `.deprecated`, OR **(b)** un-stub convex then make registry-loader async + migrate 3 callers | tracking-dedup-plan / W1 SQLite lane |
| G-A6 | 11 orphan removed-tool handler `.ts` | **DELETION_READINESS** (same gate as G-B10-8-DELETE) | same as G-B10-8-DELETE (live-runtime Codex smoke ref) | b10-8 proposal SURFACE 1 |
| G-B10-9 | accumulated-data **CLEANUP** (snapshot trim / quarantine dedup / outcome-pairs filter) | **analysis DONE 2026-06-12** (read-only); substrate is live (written by other sessions) — re-pin before any prune | a **confirmed prune pass** is run under explicit go (see B10-9 block below for the GATED disk targets + BackwardProp candidates) | tracking-dedup-plan / data-cleanup lane; report at artifacts/b10-9-bottleneck-report.md |

---

## B10-9 — accumulated-data analysis (2026-06-12)

Read-only substrate analysis (pm-off lane) is **DONE**. Headline correction: the "49,239
quarantine rejects" was mostly a **re-scan illusion** — 6 distinct poison lines re-scanned
~3,753× each. Real defects = **3 classes only**. Full analysis (pinned):
**harness-upstream/_workspace/2026-06-12-pm-cleanup-campaign/artifacts/b10-9-bottleneck-report.md**
(pin: home events.jsonl **12,181 lines @ seq 181206**, 2026-06-11T15:59:08Z).

### OPEN — BackwardProp fix CANDIDATES (actionable pm-source, pm-off lane; *candidate — verify before fixing*)

| id | candidate fix | why (evidence) |
|----|---------------|----------------|
| D1 | quarantine-and-skip — stop re-scanning the 6 poison lines | re-scan loop re-detects/re-appends same 6 lines ~3,753× each → inflates the 49k count; record once (sourceFile+line+hash) |
| D2 | protect events.jsonl from git + enforce atomic append (rule 27) | git conflict markers found INSIDE events.jsonl (3 lines) + two events glued on one line (non-atomic append) |
| D3 | route codex_lock_probe out of the 5-dim valueGrade gate | infra heartbeats (2 lines) wrongly routed through strict gate; gate fires correctly, routing is wrong |
| D4 | apply rule-05 write-to-file contract to pm_ontology_engineering_workflow | 11/14 overflow dumps from this tool = the rule-05 StructuredOutput-loop signature |
| D5 | gate outcome-pair emission on a real delta | ~99% of 22,709 outcome-pairs are empty zero-delta self-pairs (baseline==refined && score==-1) |
| D6 | fix validation_phase_completed over-emission + wire-or-retire propagationDepth | 74.4% of live log is vpc monitor noise; propagationDepth unused (0/12,181 events) |

### GATED — deferred disk cleanup (substrate is LIVE; re-pin before any prune pass)

| target | reclaim | note |
|--------|---------|------|
| snapshot rotation | ~149M | palantir-math 89M + smartall 60M, no rotation = #1 byte cost |
| quarantine row dedup | ~14M | 22,518 rows collapse to 6 poison lines |
| outcome-pairs value-filter | ~22.5k files | drop empty self-pairs (file-count cost, modest bytes) |
| overflow dumps | ~1.3M | age-out mcp-response-overflow/ dumps |
| T0 emit-gate leaks | — (quality) | hyperframes 11, palantir-math 2 — fix upstream emitter, not a disk item |

---

## DONE-ARCHIVE  (one-line closure pointers — NOT re-openable; detail in git history + rule-29 archive `reference/fable5-workflows/runs/`)

- **W1–W10 backlog** (typed-graph canonical lane, G12 137 Property prims, rule29 1.1.0, hook-coalesce no-op'd by user, W7 peerdep, F7 Korean-imperative lexicon) → DONE @ ~v7.6.0–7.7.0 (#153/#154).
- **Governed Q3 AI-derived-axes user-approval write-path** (confirm-draft) → BUILT @ v7.9.0 (#155/#156/#157).
- **M-SELF self-Ontology buildout** (30 ObjectType/33 LinkType/21 ActionType/76 Function/…) → COMPLETE, DoD **4/4** (#117–#125); FDE 0.844 PASS.
- **Altitude-2 acceptance (I-8)** + PR-I bindings → CLOSED (#145–#152).
- **Legacy-audit D3** (emit_event fan-out) / **D4** (dead-hook removal) → shipped **#144**.
- **Legacy-audit D1** (schema Phase-2) → partly **#143**.
- **Legacy-audit D5** (M-SELF reconcile) → **PR-F** shipped.
- **Legacy-audit D6** (ceremony/1b) → folded into PR-E/E2.
- **D2 (Chatbot Studio rename) → DROPPED ENTIRELY** — direction was BACKWARDS; pm's "Chatbot Studio" naming is already current ("Agent Studio" is the DEPRECATED name). Do NOT re-litigate.
- **2026-06-12 campaign:** Codex prompt-to-DTC deadweight removed; A-#3/#4 done; B10-8 schemas modernized (skill seed re-sync); tracking consolidated into THIS ledger.
- **OE-2 elevate-readiness dead-gate** — `readyForDigitalTwin` structurally always-false (all profiles `allowsDtcDraft:false`) made `register` unreachable; repaired @ **7.17.0** via `sicBackedDigitalTwinReady` (minted approved-SIC + ingested candidates, OR'd, handler-sourced never MCP-input), anti-fabrication preserved, suite green.
- **P5 gate token-bloat reduction (P1+P3+P4)** → MERGED **PR #168** (commits 7771693+489a667, squash 7b66e28; plugin **7.18.0**); per-turn ~12KB→~3KB via additive `responseView:"turn"|"readiness"` (default slim) + overflow→fullPath relocation; criterion-4 rescoped to the gate-projection sites (**Option A**, user decision); rule-04 review found no blockers (semantics/mutationAuthorized/continuity/R5 preserved). Follow-ups F-gate-dedup-B (Option B full dedup) + F-gate-overflow-gc (R2 overflow GC) logged under GATED.
- **F-gate-dedup-B + F-gate-overflow-gc** → MERGED **PR #169** (squash `e7e602a`, plugin **7.19.0**); quality-safe `decisionSpec` dedup (~8KB, turn ~35KB→~27KB, no live substance relocated) + lazy overflow-dir age-out GC. Aggressive ~3KB sparse view rejected on quality grounds (would strip live Altitude-1/2 ontology context).

---

## Per-turn context-decision fixes (from harness-upstream §9 audit) — 2026-06-20

- **STATUS = QUEUED.**
- **BLOCKED-ON** = the in-progress **harness-upstream self-MetaOptimization session** must complete before any pm CODE work starts. Do not open a pm code lane until that session lands.
- **Source (pointer, not copy — bind before do-time; do NOT inline the bodies):**
  - Audit (§6 = the 3 specs): `harness-upstream/_workspace/2026-06-20-pm-frontdoor-design-prior-injection-gap/artifacts/2026-06-20-per-turn-context-decision-audit.md`
  - Case library: `harness-upstream/reference/harness-bottlenecks/cases/` → **bd-001** (design-prior not surfaced), **bd-002** (footer keyed to prompt text), **bd-003** (protected-surface over-block keyed to vocabulary)
  - Handoff: `/home/palantirkc/harness-upstream/_workspace/2026-06-20-pm-frontdoor-design-prior-injection-gap/HANDOFF.md`

> **RUNTIME-DRIFT (re-pin before any fix):** running cache **7.21.0 @ 8a58976**; source HEAD is **AHEAD (cc5ff475)**. Any fix here bumps `plugin.json` + syncs the cache + re-confirms what executes via `installed_plugins.json` / `pm_plugin_self_check` (`runtimeIdentity.version`) — never trust source over the running cache copy.

| id | title | readiness | locus (pointer — verify live before edit) | fix direction |
|----|-------|-----------|--------------------------------------------|---------------|
| CTX-B | required response footer keyed to prompt **TEXT** not **STATE** | **READY** | `lib/ontology-engineering-response-template.ts:271-277` (+ alias `:712-713`); callers `hooks/stop-validate.ts:52`, `bridge/handlers/pm-workflow-response-validate.ts:40-49`, `hooks/prompt-front-door-capture.ts:224-231` | thread front-door **auth STATE** into the predicate; decide on STATE first, treat text markers as a recall hint only |
| CTX-iii | protected-surface **over-block** keyed to command/prompt/content **vocabulary** not **write-target** | **READY** | `hooks/ontology-engineering-workflow-enforcement-gate.ts:207-224,226-229,243-267,492-494` | introduce the sibling **write-target resolver** (`write-scope-runtime-enforce.ts:106-125`); drop `String.includes` over command/prompt/intent/content from the block decision |
| CTX-1 | design-prior **not surfaced** + **single-choice** framing | **NEEDS-DISCOVERY** | fork (a) build per-turn prior-ranking, vs (b) extend `TurnCardDecisionSpec` (`turn-card-decision-spec.ts:31-44`) with relational/cardinality/TYPE-GAP-defer; likely **both** | escalate as a **design decision**. **Schema-versioning rule 08 applies to (b)** (semver bump + CHANGELOG + generated-header gate) |

> CTX-B and CTX-iii are READY (spec-complete, mechanical). CTX-1 is NEEDS-DISCOVERY — do NOT start it as a code edit; resolve the (a)/(b) fork as a design decision first (rule 08 gates path (b)). All three remain BLOCKED-ON the harness-upstream session above.

---

## Altitude-2 drift-fold re-bind (harness-upstream Wave-6 Task 1.2) — 2026-06-21

- **DONE — pm 7.22.2 SHIPPED** (PR **#177**, merge `6e35a6e`, plugin **7.22.2**): the `rebind_registered` capability for the Altitude-2 drift-fold re-bind. **Part A** (`lib/ontology-engineering-workflow/register-accepted.ts` `reElevateAlreadyRegistered` flag → re-emit identical declaration; default byte-identical), **Part B** (`lib/event-log/read/fold-snapshot.ts` dedup-by-FULL-rid latest-wins; provable no-op on dup-free data so grammar counts never inflate), **Part C** (new **direct-caller** action `rebind_registered` + `handleRegisterRebind`; fail-closed `rebindRids ∩ live getOntology snapshot`, distinct invalidReason, reuses commitEditsHandler verbatim + handleRegister's approved&&graded gate). Builds on the merged zero-new-term-rebind gate cascade (7.22.0 #175 / 7.22.1 #176). 11 unit/handler tests + 1 e2e green; 0 NEW tsc errors. **The drift-fold register LOGIC is complete + tested + runtime-live.**
- **DEFERRED — live 13-rid re-bind NOT driven** (user decision 2026-06-21): in a FRESH session, ontology-mutating dispatch hit pm's full per-prompt Altitude-1 governance wall — approved SIC + DTC are persisted, but this session's prompt-envelope→`digital_twin_approved` + `mutationAuthorized` (work-contract + decision-record) were not re-established; the Agent dispatch was hard-blocked. No bypass (forbidden). The 13 harness-upstream primitives stay lineage-stale at `atopWhich=def3d440`.
- **NEW pm follow-up (operability gap) — STATUS QUEUED:** `rebind_registered` is direct-caller (NOT MCP-exposed, like register/ingest/lint) AND has **no streamlined governed trigger**; a fresh session must re-run the full Altitude-1 ceremony to drive it. Proposed: a composed GOVERNED flow (like `elevate`) — e.g. `drift_rebind` — that, given a verified re-bind rid set + the persisted approved SIC/DTC, advances the gate AND drives `rebind_registered` in ONE governed MCP call; or a resume helper that re-binds a persisted approved SIC/DTC to the current prompt envelope. (Operability lesson, NOT a surface-text-vs-state case.)
- **NEW evidence for CTX-iii / bd-003 — broaden the fix scope:** the vocabulary-keyed over-block RECURRED on a **memory-note Write** (outside the project) via the **FDE-provenance / SIC-authoring** gate, not just the `:207-224` protected-surface gate. The CTX-iii write-target-resolver fix must also cover that sibling gate. Evidence line appended to `harness-upstream/reference/harness-bottlenecks/cases/bd-003-protected-surface-overblock-keyed-to-vocabulary.md` (## RECURRENCE).

---

## Bottleneck-improvement follow-ups — 2026-06-21

- **DONE — pm 7.22.3 SHIPPED** (PR #179, merge `fca23f6`): surface-text-vs-state gate bundle. **CTX-B** (response-template footer → STATE-first) and **CTX-iii** (enforcement-gate write-target resolver, incl. resolved-Bash-write-target extraction `03c181d` that closes the protected pm-source Bash-write vector) from the 2026-06-20 CTX queue are now LANDED, plus **cluster A** (tool-classifier denylist-wins). 0 NEW tsc; adversarial + whole-Altitude e2e verified (caught + closed a Bash-write under-block regression mid-flight). Also DONE: **C** version-sync assertion + CI tsc gate + marketplace.json resync (PR #180); **G-leak** `*:Zone.Identifier` ADS deleted (harness-upstream; 1 leaked a claude.ai UUID, never committed).
- **STILL QUEUED** (verified 2026-06-21; evidence: `harness-upstream/_workspace/2026-06-21-bottleneck-cluster-verification/{A..H}-*.md`):
  - **E drift_rebind** — the composed GOVERNED flow to drive the deferred live 13-rid Altitude-2 register (resume persisted approved SIC/DTC → advance prompt-DTC + OE mutationAuthorized → `rebind_registered`, one fail-closed call). Already queued above; **recommended next substantive item**. The 13 stay stale at `atopWhich=def3d440`.
  - **D outcome-pairs degeneracy** (S) — ~96.8% of outcome-pairs are degenerate self-pairs (baseline==refined, score −1, instant-close). Fix: skip emitting a pair when `baseline==refined && score==−1` (or require a populated baseline + real score before close) in the outcome-pairing writer. Do NOT touch existing pairs (append-only). The events.jsonl pollution/size/sqlite sub-claims were REFUTED (log is clean: ~2.3M / ~1257 lines, 5-dim complete).
  - **A-secondary tool-classifier verbs** (S) — widen `lib/hooks/tool-classifier.ts` mutating denylist with pre-existing-gap write verbs NOT yet classified MUTATION: sponge, python/node writer scripts, `curl -o/-O`, wget, `gh api -X POST`/`gh release create`/`gh repo delete`, `git worktree add`/`config`/`update-ref`, `tar xf`, unzip, `kubectl apply`, `docker run`. Pre-existing (NOT a 7.22.3 regression); safe direction.
  - **C-followups** (S–M) — (a) add a **bun-test BLOCKING CI gate**: needs a known-failures-aware wrapper (parse `plugins/palantir-mini/tests/KNOWN_BROAD_SUITE_FAILURES.md`, fail on NEW) + clear the ~29-failure debt; (b) collapse the **3** redundant `marketplace.json` copies to one canonical; (c) plugin **README** `Current release: v6.79.0` line is stale (current 7.22.3) — narrative, deliberately excluded from the version-sync assertion; bump or remove.
  - **H label** (cosmetic) — `hooks/session-start.ts` (~:73) hardcodes a stale `"claude-opus-4-6"` event-label fallback; derive from `p.model` (reference-don't-pin). The 5 seeds `model:"sonnet"` vs opus-only is the existing **P3** (owner-gated; pm-plugin policy may legitimately differ).
  - **CTX-1** (NEEDS-DESIGN; already in the 2026-06-20 CTX queue) — design-prior injection + single-choice framing; resolve the (a) per-turn prior-ranking vs (b) extend `TurnCardDecisionSpec` fork as a design decision first (schema **rule 08** gates fork b). Do NOT patch.
- **Owner-decision (harness-upstream, not pm):** G-content — ~11.78MB committed marketing PNG + personal KakaoTalk screenshots + top-level debug/marketing HTML (commit `698705e` / PR #91) vs the "hosts no project content" invariant.
- **Full handoff:** `harness-upstream/_workspace/2026-06-21-bottleneck-cluster-verification/HANDOFF-remaining-backlog.md`.

---

## Bottleneck-cluster closure — 2026-06-21 (pm 7.22.4 + 7.23.0 shipped; verify live)

> Closure pass for the 2026-06-21 bottleneck track. Versions/SHAs/counts are POINTERS —
> verify live (`plugins/palantir-mini/.claude-plugin/plugin.json`, `git log`, CHANGELOG).

- **DONE — pm 7.22.4 SHIPPED** (PR #182): bottleneck bundle.
  - **D outcome-pairs degeneracy → RESOLVED.** Degenerate-pair skip guard added in **BOTH**
    write paths — `lib/outcome-pairing/track.ts` (the `writeClosedMarker` path) **and**
    `hooks/outcome-pair-tracker.ts` — so a pair is not emitted when `baseline==refined &&
    score==−1`. Existing pairs untouched (append-only).
  - **A-secondary tool-classifier verbs → RESOLVED.** Mutating denylist widened with the
    pre-existing-gap write verbs (sponge / curl -o / wget / gh api-mutate / gh release / gh repo
    / git worktree·config·update-ref / tar -x / unzip / kubectl / docker). **No blanket
    `python|node`** — the anti-over-block posture is preserved.
  - **H-label → RESOLVED.** Session-start now emits `p.model`; the stale `claude-opus-4-6`
    literal is dropped and `SessionStartedEnvelope.payload.model` is made optional
    (reference-don't-pin).
  - **P3 agent seeds → RESOLVED (done).** The 5 agent-definition seeds
    (`implementer`/`hook-builder`/`plugin-maintainer`/`protocol-designer`/`project-implementer`)
    flipped `model:"sonnet"` → `"opus"`, aligning with the opus-only policy. **The OPEN-table P3
    row is now closed by this entry.**
- **DONE — pm 7.23.0 SHIPPED** (PR #183): **E `drift_rebind`** — the composed GOVERNED RESUME
  action is built + merged. It re-binds a persisted **MINTED approved** SIC+DTC to the current
  prompt envelope, which drives the unchanged fail-closed `rebind_registered` (single commit
  boundary; **no** mint, **no** bypass, **no** new rid; emits a `drift_rebind_envelope_advanced`
  audit event). Passed adversarial governance + correctness review; also cleared a pre-existing
  executor-registration fingerprint drift. **→ The `rebind_registered` operability follow-up
  logged in the §"Altitude-2 drift-fold re-bind" section above (the "NEW pm follow-up
  (operability gap) — STATUS QUEUED" bullet) is now RESOLVED via `drift_rebind`.**
- **STILL OPEN after this closure:**
  - **LIVE 13-rid Altitude-2 register** — now a SINGLE `drift_rebind` MCP call once pm 7.23.0 is
    installed + ON. Preconditions verified present in `~/harness-upstream/.palantir-mini`
    (approved SIC `altitude-2-dynamic-drift-re-bind-re-elevate-atop` + approved DTC; the 13 rids
    = `get_ontology` ∩ SIC; `atopWhich` auto-stamped from git HEAD at commit). **Recommended next
    substantive item.**
  - **FULL pm MCP/Skill/Agent/Hook MetaOptimization RE-REVIEW** — user-directed, explicitly
    sequenced to run **AFTER** the live 13-rid register completes (user sequencing this session).
    Carry-forward observation for it: the tool-classifier over-blocks **read**-subcommand forms
    (e.g. `git config --get`, `git worktree list`) consistent with the file's PRE-EXISTING
    convention (`git tag` / `git stash` list forms already over-blocked) — a classifier-precision
    pass candidate, NOT fixed in 7.22.4 (surgical scope: 7.22.4 only widened the MUTATION
    denylist).
  - **C(a) bun-test CI gate → BLOCKED.** `plugins/palantir-mini/tests/KNOWN_BROAD_SUITE_FAILURES.md`
    (last refreshed 2026-05-11) is **doubly stale** at pm 7.23.0: ~14/15 listed entries now PASS,
    and **5** genuinely-failing test files are MISSING from it —
    `tests/docs/reload-per-runtime.test.ts` (doc drift: Codex-only vs Codex+Claude),
    `tests/lib/codex/palantir-mini-activation-policy.test.ts`,
    `tests/ontology/self/hook-registration.test.ts`,
    `tests/ontology/self/plugin-manifest-registration.test.ts` (expects 45 skills, got 44),
    `tests/ontology/self/skill-registration.test.ts`. These 5 are **PRE-EXISTING drift on pm
    main, NOT from this session.** Unblock path: refresh the ledger (drop now-green rows;
    triage/fix the 5 red files), THEN land the validated wrapper (JUnit-reporter based; known-set
    parsed by `tests/` path; NEW = failing − known) + a `package.json` `test:ci` script + a
    blocking step in `.github/workflows/palantir-mini-integrity.yml`.
  - **F giant-file split** — modular split of `pm-semantic-intent-gate.ts` (~3k) +
    `lib/lead-intent/contracts.ts` (~2.4k) deferred (cohesive/test-partitioned; `contracts.ts`
    fan-in ~54 + rule-08 exposure). Do when a relational-card change forces it.
  - **marketplace.json 3-copy collapse** — deferred (low value; root vs plugin copies serve
    different host-read paths; CI version-sync already keeps them consistent).
- **DONE elsewhere this session (cross-repo, recorded for completeness):**
  - **C(c)** marketplace README de-pinned (Current-release line → references `CHANGELOG.md`;
    PR #184).
  - **G-content** harness-upstream orphan removal (5 files) + the CTX-1 design-decision doc
    (harness-upstream PR #97); banners + `index/privacy.html` retained.
  - **KakaoTalk history purge** — 9 `KakaoTalk_20260619_*.jpg` purged from harness-upstream
    history (filter-repo + force-with-lease).
- **Full handoff:** `harness-upstream/_workspace/2026-06-21-bottleneck-cluster-verification/HANDOFF-remaining-backlog.md`.

---

## 2026-07-11 — pm 7.42.0 governance-gate gaps (P1 unification session evidence)

Session evidence: home session 814cfd8c (Lead) + marketplace M1b executor session. All four are pm-source defects in the prompt-front-door / prompt-DTC gate stack; lane pm-off unless noted.

| id | title | summary | acceptance | source |
|----|-------|---------|------------|--------|
| G-ENV-A | per-session/per-project envelope-store isolation | UserPromptSubmit capture writes ONLY the cwd-resolved project store, so (i) dispatched-subagent hook payloads cannot resolve the parent session's approved envelope and (ii) a multi-repo session has NO envelope in the second repo's lane (empirical: home lane captured, marketplace lane "No current prompt-front-door envelope is available" same turn). M1b workaround: SendMessage resume text is captured as a prompt envelope in the agent's cwd lane. Second empirical confirmation same day: a fresh home-session executor's Edit on an /ontology/ path-class test file hard-blocked with no envelope resolvable in the marketplace lane (see G-ENV-B interim unlock). | subagent/second-repo lanes resolve the parent session's envelope (cross-lane pointer or parent-session fallback), covered by a gate test | P1 unification session 2026-07-11; M1b implementer report §Governance store artifacts; **RESOLVED 2026-07-12: PR #215, pm 7.45.0 — global session index pointer-redirect (new lib/prompt-front-door/global-session-index.ts, GLOBAL (runtime, sessionId)-keyed store, never envelope copy), capture-hook best-effort dual-write, fail-closed gate fallback on local miss only; spike-grounded (runtime, sessionId) key (subagent PreToolUse payload.session_id == parent session's, claude CLI 2.1.207)** |
| G-ENV-B | A2 authorized-delivery lane unreachable on Claude-native harness | The documented re-issue lane (tool_input.userApprovalQuote/userApprovalPromptId/userApprovalPromptHash) is rejected by the Claude harness with InputValidationError (Bash schema additionalProperties:false) — extends the known Edit/Write continuity-fallback gap to ALL native tools; the A2 lane is dead code on the Claude runtime. Evidence: re-issued git call rejected pre-gate; bun -e verifyDeliveryApprovalAgainstEnvelope with identical values returned authorized:true (fresh, on-current-turn). Interim unlock: user turn carrying an explicit plugin opt-out marker. | a Claude-native-reachable authorization channel exists (e.g. turn-scoped pre-authorization persisted via a pm CLI/MCP tool that the PreToolUse gate consults) + gate test proving a Bash delivery call passes with it | g12 row de-2026-07-11-p1-h1b-m1b-shipment-deferred-one-turn; **structural on Claude-native (harness-owned additionalProperties:false schemas); resolution folds into G-DSN-E design — slice 3** **RESOLVED 2026-07-12: PR #216, pm 7.46.0 — folded into G-DSN-E (standalone MCP tool pm_authorize_delivery); dead tool_input re-issue lane retained as Codex-lane fallback, deny hint rewritten to name pm_authorize_delivery as primary** |
| G-ENV-C | drift_rebind throws on already-approved envelope | drift_rebind throws when the envelope is already digital_twin_approved instead of no-op'ing the advance step (hit twice in M1b; recovered via Stage-06 + workflow turn). | drift_rebind is idempotent on approved envelopes; regression test | M1b implementer report deviation note 3(c); **RESOLVED 2026-07-12: PR #213, pm 7.44.0** |
| G-CLS-D | delivery-classifier prose false-positive | A Bash command whose quoted ARGUMENT STRINGS contain delivery verbs (푸시/머지/push/merge) is classified as a delivery action even when the executable performs none (empirical: a decisions:emit governance-ledger append was hard-blocked until its prose arguments were neutralized). Related to the known read-subcommand over-blocking precision candidate. | classifier keys on executable/argv semantics, not raw substring scan of quoted argument prose; regression fixture with delivery verbs inside quoted args | home session 814cfd8c, 2026-07-11; **RESOLVED 2026-07-12: PR #213, pm 7.44.0** |
| G-CLS-E | delivery-classifier variable-indirection false negative | A Bash command that assigns a delivery-shaped string to a variable and invokes it indirectly (e.g. `CMD="git push"; $CMD origin main`) no longer classifies as delivery post quote-awareness (G-CLS-D) — it was previously only ever caught by accident via raw substring scan, never by design. | classifier resolves shell-variable indirection before matching delivery verbs, or explicitly declares indirection out-of-scope with a documented residual-risk note; regression fixture covering `CMD="git push"; $CMD origin main`. **Recommended fix locus: `lib/hooks/tool-classifier.ts` shared architecture.** | PR #213 verifier findings, 2026-07-12 |
| G-DSN-E | authorization UX: flexibility over phrase-marker dependence (USER directive) | USER directive 2026-07-11 (mid-turn, session 814cfd8c; g12 row de-2026-07-11-authorization-flexibility-over-phrase-markers): the gate stack depends too heavily on exact phrase markers (approval-verb/delivery-marker/opt-out lexicons, verbatim-substring quotes) and the rigidity wastes tokens. Same-turn empirical proof: (i) a mid-turn user message superseded the current-pointer envelope and silently revoked an active opt-out, re-blocking mid-flight; (ii) the SendMessage-resume capture workaround cited in G-ENV-A did NOT reproduce for either repo lane (both executors re-blocked, "No current prompt-front-door envelope"); (iii) gate lane resolution keys on harness payload.cwd, ignoring in-command cd/-C targets. Governs the fix design for G-ENV-B and G-CLS-D. | common-path authorization uses structured, runtime-reachable turn/session-scoped grants; wording lexicons demoted to fallback | g12 de-2026-07-11-authorization-flexibility-over-phrase-markers; **RESOLVED 2026-07-12: PR #216, pm 7.46.0 + schemas-snapshot 1.97.0 — standalone MCP tool pm_authorize_delivery (bridge/handlers/pm-authorize-delivery.ts) reusing verifyDeliveryApprovalAgainstEnvelope verbatim; new lib/prompt-front-door/delivery-grant-store.ts (SESSION scope, 30-min TTL, TTL-only expiry, no revocation v1); gate A2 lane consults the grant store first (hasLiveDeliveryGrantForGate), existing tool_input re-issue + lexicon lanes retained byte-identical as fallback; grant key derives from the resolved envelope only; e2e 9/9 verified (S1-S9, independently re-run: cross-lane allow, backdated-expiry re-block, forged-quote refusal, zero real-home pollution)** |

---

## 2026-07-12 — pm-flex slices 2-3 follow-on intake (pm 7.45.0/7.46.0 e2e evidence)

Session evidence: home Lead session 979e0319, run `2026-07-12-pmflex-s23` (G-ENV-A/G-ENV-B/G-DSN-E resolutions above are from this same run). Discovered while gathering e2e gate evidence for slice 3; none are gates on the slice-2/3 shipment itself — surfaced for future-touch tracking.

| id | title | summary | acceptance | source |
|----|-------|---------|------------|--------|
| G-EVT-F | `delivery_authorization_granted` event row not observed in e2e scratch run | The e2e scenario (S4, real `pm_authorize_delivery` handler mint) did not produce an observable `delivery_authorization_granted` row in `events.jsonl` during the scratch run. Verifier judged this cosmetic (the handler path was exercised via direct import, not the live MCP transport, in that run) rather than a functional defect. | a live-runtime (pm re-enabled) run of the same e2e scenario shows the `delivery_authorization_granted` row landing in `events.jsonl` per rule-10 5-dim shape | pm-flex slice-3 e2e gate verification, home session 979e0319, 2026-07-12; artifact `_workspace/2026-07-12-pmflex/reports/e2e-gate-verification.json` (machine-local); **STILL OPEN 2026-07-12 (G-GATE-J investigation): re-grepped both `~/projects/.palantir-mini/session/events.jsonl` and this repo's `.palantir-mini/session/events.jsonl` for `delivery_authorization_granted` — zero rows in either; the only hit anywhere is prose in a `cartography_decision_mirrored` payload referencing this row as a still-pending TODO. Live row has not yet landed on any real mint; left OPEN pending a live-runtime mint that actually emits it.**; **RESOLVED 2026-07-12: a real live-transport `pm_authorize_delivery` mint (grant `dg-mrh6p3o7-19e5c6ca6aca`, session `76c8ea65`) landed a `delivery_authorization_granted` row `evt-mrh6p3ol-buenyez7` at 2026-07-12T02:38:27.045Z in the HOME-ROOT lane `/home/palantirkc/.palantir-mini/session/events.jsonl`, rule-10 5-dim shape confirmed (eventId/when/atopWhich/throughWhich{sessionId,toolName,cwd}/byWhom/withWhat). Root-cause of the earlier "not observed": lane-selection miss — prior greps checked only the `projects/` and marketplace lanes; the grant event lands in the lane of the `project` argument passed to the mint.** |
| G-MCP-G | MCP transport/schema layer for `pm_authorize_delivery` untested live | The e2e evidence drove `pm_authorize_delivery` via direct handler import, not through the actual MCP tool-call transport/schema validation layer a real Claude session would use. The handler logic (verbatim `verifyDeliveryApprovalAgainstEnvelope` reuse) is proven; the transport wrapper around it is not. | a live-runtime (pm re-enabled) MCP tool call to `pm_authorize_delivery` succeeds end-to-end through the real transport, not a direct import | pm-flex slice-3 e2e gate verification, home session 979e0319, 2026-07-12; **RESOLVED 2026-07-12: live user-attested smoke — a real `gh pr merge` on a home-repo governance-touching PR exercised the `pm_authorize_delivery` MCP tool through the actual transport (not a direct import); the transport wrapper is proven live. (The SAME live smoke is what surfaced G-GATE-J: the grant was minted but the gate's change-set proof measured the wrong object, so the merge still blocked — a separate, now-fixed defect from the transport question this row tracks.)** |
| G-TEST-H | 2 pre-existing full-suite test failures on marketplace `main` | Independently reproduced on a clean `main` checkout (unrelated to pm-flex slices 2-3): (i) rule-26 doc-grading-conformance version-lockstep check, and (ii) fresh-home `CORE.md` line-count check — both assert against LIVE `~/.claude` home-dotfile content rather than repo-local fixtures, so they are environment-coupled rather than code defects. | either the two checks are re-scoped to repo-local fixtures (removing the live-home coupling), or explicitly documented as environment-dependent and excluded from the plugin's own CI gate | pm-flex slice-3 e2e gate verification, home session 979e0319, 2026-07-12; reproduced independently on clean `main` |
| G-FETCH-I | marketplace clone fetch refspec narrowed to `main` only | The local marketplace clone's `origin` remote has `fetch = +refs/heads/main:refs/remotes/origin/main` (not the default `+refs/heads/*:refs/remotes/origin/*`), so `origin/<feature-branch>` refs never materialize locally after a push — diffing or comparing against another session's just-pushed branch requires a full unshallow/refspec widen or working from the local branch directly. | `.git/config` `remote.origin.fetch` is widened to track all branches, or this constraint is accepted and documented as a standing note for cross-session branch comparison | pm-flex slice-3 e2e gate verification, home session 979e0319, 2026-07-12 |
| G-LANE-L | gate lane resolution keys on harness `payload.cwd` (persistent shell cwd) | (empirical, session `76c8ea65` 2026-07-12T02:58:46Z): a `cd /home/palantirkc && git push origin <branch>` from a subagent whose PERSISTENT shell cwd was `/home/palantirkc/palantir-mini-marketplace` was gate-evaluated in the MARKETPLACE lane — the deny text named `Bash in /home/palantirkc/palantir-mini-marketplace` — so the push-range proof ran against the wrong repo (synced marketplace `main` → empty range → conservative blocking floor → the A2 grant lane unreachable pre-7.48 per G-GATE-J), hard-blocking a user-approved HOME-repo ship despite a live delivery grant (`dg-mrh7ebff`). This re-confirms G-DSN-E evidence item (iii) (lane keys on `payload.cwd`, ignoring in-command `cd`/`-C` targets) as a standalone defect now that G-GATE-J/G-DOCS-K are fixed; also note the same whole-command-string architecture flagged in G-CLS-E. Operational workaround of record: pin the persistent shell cwd to the mutated repo's root (standalone `cd`) before any delivery command; push new branches WITHOUT `-u` so the `origin/main` fallback range resolves. | lane resolution honors the repo actually mutated (e.g. `resolveGitTargetRepo`-style parsing extended to push/pr surfaces, or `payload.cwd` corrected by in-command `cd` resolution) + a regression fixture reproducing the cross-repo `cd` case | home session `76c8ea65` ship-attempt transcript, 2026-07-12 |
| G-RPLY-M | approval anti-replay window slides past the genuine approval in autonomous sessions | (empirical, home session `76c8ea65`, 2026-07-12 ~03:3xZ): `pm_authorize_delivery` re-mint anchored to the SAME kickoff envelope that had already minted 3 grants earlier in the session was REFUSED with "approval promptId is neither the current nor the immediately-previous front-door pointer (stale/replay)" — because system task-notifications (background-agent completion events, NOT user turns) are captured by the prompt front door as prompts, sliding the current/previous pointer window past the genuine user-approval envelope. Consequence: in long autonomous stretches (background workflows reporting in), a standing user delivery approval becomes unreachable for re-mints even though the user's authorization is unchanged and unexpired in intent. Interim behavior of record: fail-closed refusal is correct anti-replay behavior; operational workaround = mint immediately before each delivery step while the approval envelope is still within the window, and rely on the non-grant de-floor path (proven non-ontology range from the correct lane) otherwise. | the anchor window distinguishes USER-authored prompts from system/notification captures (e.g. only user-turn envelopes advance the anti-replay window, or approval envelopes get a bounded validity independent of pointer position) + a regression test covering a notification-interleaved re-mint. | home session `76c8ea65` 2026-07-12; relates G-DSN-E (flexibility directive) + delivery-grant store TTL design (30-min upper bound, g12 de-2026-07-12-pm-flex-slices-2-3-execution-rulings-of). **FIX IN REVIEW 2026-07-12: redesigned as (i) an origin-classifier (lib/prompt-front-door/prompt-origin-classifier.ts) gating a new current-user-authored pointer file that only advances on user-authored turns, wired into verifyDeliveryApprovalAgainstEnvelope's check #5 anchor, and (ii) the pm_authorize_delivery grant made session-standing (24h safety-net TTL, revocable via a user-authored revoke phrase, supersedes the 30-min absolute-TTL ruling in g12 de-2026-07-12-pm-flex-slices-2-3-policy-answers-locked per this session's directive of record) — see design-pm-auth-friction.md, landing PR TBD, pm 7.50.0.** |

## 2026-07-12 — G-RPLY-M redesign + G-ADV-N (pm 7.50.0)

Design of record: `~/projects/_workspace/2026-07-12-w10-closure/design-pm-auth-friction.md`. One PR, two sequential slices (both touch `hooks/gates/prompt-dtc-enforcement-gate.impl.ts` at disjoint line ranges). G-RPLY-M's fix is annotated inline on its row above (pm-flex slices 2-3 table); the new row below is G-ADV-N, discovered and fixed complete in this same PR.

| id | title | summary | acceptance | source |
|----|-------|---------|------------|--------|
| G-ADV-N | PreToolUse advisory boilerplate re-emitted byte-identical on every matching call, no session dedup | `hooks/gates/prompt-dtc-enforcement-gate.impl.ts`'s scoped-advisory / SIC-cache-miss / generic-and-delivery-blocking branches re-emit 4 byte-identical filler lines ("Allowed while pending: ...", "Pass condition: ..." [either variant], the plain `MODE=off` escape line, or the "Default selective-blocking gates..." line) on every non-cached matching mutating call — at the session's own reported frequency of hundreds of times, for BOTH the Lead session and every dispatched subagent sharing its `session_id`. | a session-scoped dedup store collapses the 4 identified filler lines to a `<=10`-token marker after first occurrence per `(runtime, sessionId, boilerplateClass)`, while `assessment.reason`/the runbook pointer/the `pm_authorize_delivery` authorize-delivery paragraph stay full every call | this session's live evidence + `scout-mp-advisory.md`; **FIXED SAME PR 2026-07-12: new `lib/prompt-front-door/advisory-shown-store.ts` (session+class-keyed dedup, `PALANTIR_MINI_GLOBAL_STATE_DIR`-overridable, fail-open) backs a `collapsibleBoilerplateLines` helper wired into all 3 call sites in `hooks/gates/prompt-dtc-enforcement-gate.impl.ts`; regression coverage in `tests/lib/prompt-front-door/advisory-shown-store.test.ts` + `tests/hooks/prompt-dtc-enforcement-gate-advisory-dedup.test.ts` (first-occurrence full text, second-occurrence marker, per-class independence, cross-session isolation, delivery-authorize paragraph always full, fail-open on store corruption) — pm 7.50.0.** **ROI-correction (Session-283c2b45 token audit, 2026-07-12): advisory text reached EXECUTOR contexts only on genuine BLOCK (3x); Lead main-loop context was affected on every mutating call — dedup value concentrates on the Lead surface.** |
| G-HOOK-O | pm hook returns a non-conforming PreToolUse JSON shape, failing schema validation on every invocation | Session-283c2b45 token audit (home repo `_workspace/2026-07-12-w10-closure/token-waste-audit.md`): a pm PreToolUse hook's JSON response failed schema validation on 92/92 invocations this session, producing ~34K tokens of dead log output with no functional payload delivered. Hook identity not yet isolated. | the offending hook is identified (candidate: whichever PreToolUse hook's stdout was captured in the audit) and its JSON response is brought into schema conformance; regression test asserting the response shape validates cleanly | Session-283c2b45 token audit, `token-waste-audit.md`, home repo `_workspace/2026-07-12-w10-closure/`, 2026-07-12; **FIX-IN-REVIEW 2026-07-12 (pm 7.51.0, W5 branch `claude/w5-token-diet-7-51`, not yet merged): scope correction — 7 production files needed the fix, not the 1 scout-confirmed offender (`hooks/ontology-engineering-workflow-enforcement-gate.ts`, `.impl.ts`, `hooks/prompt-dtc-enforcement-gate.ts`, `hooks/ontology-import-guard.ts`, `hooks/write-scope-runtime-enforce.ts`, `hooks/researcher-citation-precision.ts`, `hooks/value-grade-assigner.ts`, `hooks/semantic-frontmatter-validate.ts`); full file list + rationale: `~/projects/_workspace/2026-07-12-w10-closure/design-w5-token-diet.md` §Slice 1; new table-driven regression test `tests/hooks/pretooluse-output-shape-conformance.test.ts`; CHANGELOG `[7.51.0]`.** |
| G-SKILL-P | pm skill catalog injects ~1.2K tokens per agent spawn when enabled | Session-283c2b45 token audit: the pm skill catalog (40 skills) accounts for ≈1.2K tokens (26% of skill_listing mass) injected into every agent spawn when the catalog is enabled. | a catalog diet (fewer exposed skills and/or shorter per-skill descriptions) reduces the per-spawn skill_listing token mass, measured against this session's baseline | Session-283c2b45 token audit, `token-waste-audit.md`, home repo `_workspace/2026-07-12-w10-closure/`, 2026-07-12; **slice A (description shortening) LANDED 2026-07-12 (pm 7.51.0, W5 branch `claude/w5-token-diet-7-51`, not yet merged): `scripts/slim-skill-frontmatter.ts` re-run over 7 over-cap skills, 253 raw chars / ≈63 tokens-per-spawn saved, `pm-research` hand-overridden to keep its "audit" trigger word — see CHANGELOG `[7.51.0]`. What remains open: slice B (remove/merge candidates table, design doc §Slice 4) is a separate USER-GATED decision, not executed in this PR — row stays open pending that decision.** **slice B EXECUTED 2026-07-13 (pm 7.52.0, branch `claude/w5b-skill-merges`): USER approved merges 1-3 of the design doc §Slice 4 table (session 283c2b45) — `pm-ontology-proposal-create`+`pm-ontology-proposal-review`+`pm-ontology-branch-create`+`pm-ontology-drift-propose` -> `pm-ontology-proposal` (modes create\|review\|branch\|drift); `pm-walk-analyze`+`pm-walk-build` -> `pm-walk` (modes analyze\|build); `pm-rule-memory-prune` absorbed into `pm-rule-audit`'s new `prune` mode (name kept); catalog 44 -> 39 skills, content-lossless (every absorbed skill's full body preserved as its mode section) — see CHANGELOG `[7.52.0]`. Item 4 (`pm-impact-quick` + `pm-pr-impact`) was explicitly NOT approved this turn and remains untouched, kept separate. Row stays open only if further catalog-diet levers are identified; slice B as scoped is complete.** |
| G-DEAD-Q | two boilerplate call sites in `prompt-dtc-enforcement-gate.impl.ts` reported as wired-but-dead (slice-2 finding, locus unconfirmed) | Slice-2 (G-ADV-N advisory-dedup wiring) reported two call sites constructing/passing boilerplate text into the gate's advisory-assembly path as structurally unreachable. **W4 verification note (2026-07-12): a direct grep of the file as currently merged found exactly 3 `collapsibleBoilerplateLines` wiring sites (scoped-advisory / sic-miss / the delivery-vs-generic-blocking ternary), all reachable under different mode/branch conditions, and no orphaned duplicate boilerplate text blocks outside those 3 sites — the reported locus does not reproduce against the current file state and needs re-confirmation (stale finding from an earlier diff state, or a different file/definition of "call site") before disposition. | re-locate/reproduce the finding against the current file, THEN decide fix (make reachable) vs accept-as-legacy (remove/annotate) vs close-as-stale | slice-2 implementation finding, pm 7.50.0 landing PR, 2026-07-12; W4 re-verification same session; **W5 slice-5 RE-VERIFICATION 2026-07-12 — CORRECTS the "all reachable" conclusion above, disposition is NOT RESOLVED-STALE: N1 scout's independent re-check (`scout-w5-hooks.md` §2) counted 4 textual `collapsibleBoilerplateLines(` call sites (scoped-advisory L1730, sic-miss L1778, delivery-blocking L1835, generic-blocking L1843) and cited `tests/hooks/prompt-dtc-enforcement-gate.test.ts:714,743,759` as reachability evidence for all 4. Re-verified here by DIRECT EXECUTION of `promptDtcEnforcementGate` (not grep/reading alone, throwaway probe payloads, not checked in): delivery-blocking and generic-blocking ARE live-confirmed reachable (Edit/commit_edits/apply_edit_function payloads under `PALANTIR_MINI_PROMPT_DTC_GATE_MODE=selective-blocking` all landed in these two classes). scoped-advisory and sic-miss did NOT fire for any payload tried — root cause traced in `lib/governance/effective-gate-mode.ts`: `PROJECT_GATE_POLICY_MINIMUMS` maps all 6 `ProtectedMutationClass` values to a floor of `scoped-blocking` or `blocking`, none to `selective-blocking` or lower. `protectedMutationClassForPromptGate` (impl.ts:211-284) assigns a defined `ProtectedMutationClass` (hence a floor >= scoped-blocking) to EVERY payload that is either `mutating` (unconditional `generic-mutation` fallback, impl.ts:282) or `isOntologyAffectingForSelectiveBlocking` (unconditional `ontology-write` catch-all, impl.ts:228, independent of the `mutating` flag). Since `mode = resolveEffectiveGateMode(...).effectiveMode` (impl.ts:1637) is computed BEFORE the `mode === "selective-blocking"` dispatch (impl.ts:1715), and reaching that dispatch with `mode` still equal to `"selective-blocking"` REQUIRES both `mutating=false` AND `isOntologyAffecting=false` (else the floor would have already strengthened it), the two inner conditions gating scoped-advisory (`mutating && scopedBlocking.scoped`, impl.ts:1719) and sic-miss (reached only when `isOntologyAffecting=true`, impl.ts:1751) can never both hold — they are STRUCTURALLY UNREACHABLE under the current floor table, not merely under-tested. The 3 cited tests (:714/:743/:759) are consequently mis-titled: their payloads (generic Edit / commit_edits / apply_edit_function) are all classified mutations whose floor strengthens `mode` to `scoped-blocking`/`blocking` before the dispatch runs, so they actually exercise generic-blocking/delivery-blocking (already known-reachable) under a loose `.toContain("BLOCKING")` substring assertion that does not discriminate `"SELECTIVE-BLOCKING"` from `"SCOPED-BLOCKING"`/`"BLOCKING"` — none of the three ever reach the branches their test names claim. Disposition: REOPENED (not closeable this PR) — 2 of 4 sites confirmed live-reachable (no action), 2 of 4 (scoped-advisory, sic-miss) are dead code requiring an explicit decision: either (a) confirm "selective-blocking" is intentionally a no-op for any classified mutation and remove/annotate the 2 dead branches + their mis-titled covering tests as legacy, or (b) add a genuine `ProtectedMutationClass` floor exception (or a new unclassified-but-scoped case) that lets `selective-blocking` survive for some real payload, paired with a test that reaches it via direct invocation (not title-only). The design-noted "dedup-collapse coverage gap for scoped-advisory/sic-miss" (design-w5-token-diet.md §Slice 5) is MOOT until (a)/(b) is decided — you cannot assert dedup-collapse behavior on a branch that cannot be entered even once.** |
| G-HOME-R | `scripts/log.ts` `resolveEmitRoot()`'s excluded-root fallback was a no-op when `cwd` WAS the excluded root itself, leaking test/handler writes into the real `~/.palantir-mini` | A full unscoped `bun test` run (beyond the 7 files G-HOOK-O's test-home-isolation fix scoped) still wrote new files under the real `~/.palantir-mini/session/` (events.jsonl appends + `outcome-pairs/*.json`). Root cause: when a handler-supplied `cwd` is ALREADY an excluded root (real HOME, satisfied by HOME's own genuine `.palantir-mini` marker), the "found root is excluded -> fall back to startDir" branch returned `startDir`, which in that case IS the same excluded dir — a no-op exclusion whenever `cwd === HOME` exactly. | `resolveEmitRoot()` also checks `isExcludedProjectRoot(startDir)` in that fallback and routes to a dedicated `os.tmpdir()`-rooted scratch dir instead of `startDir` when it fires; regression tests cover both the excluded-root routing and that normal registered/home resolution order is unchanged; `scripts/verify-real-home-untouched.ts`'s snapshot/diff is a FULL RECURSIVE walk (path+size+mtime per file) so nested leaks 2+ levels deep are caught, not just top-level dir mtimes | review finding during the same W5 landing that shipped G-HOOK-O's test-home isolation fix, 2026-07-12; **FIXED SAME PR 2026-07-12 (pm 7.51.0, W5 branch `claude/w5-token-diet-7-51`): `resolveEmitRoot()` fix + 2 new regression tests in `tests/scripts/log.test.ts` ("excluded startDir (real HOME) does not resolve to HOME itself", "emit() with cwd=HOME writes under the tmpdir scratch root, never under the real ~/.palantir-mini") + `scripts/verify-real-home-untouched.ts` rewritten to a full recursive per-file walk (proven against a throwaway 2-deep append + 2-deep new-file fixture on the real tree, fixtures removed after); CHANGELOG `[7.51.0]` third `Fixed` bullet.** |

---

## 2026-07-12 — gh-pr-merge delivery-proof gap + docs-class carve-out (pm 7.47.0)

Live smoke finding (user-reported): a live `pm_authorize_delivery` session grant was NOT honored by the prompt-DTC gate on a `gh pr merge` of a governance-touching home PR. Lead-verified mechanism in `hooks/gates/prompt-dtc-enforcement-gate.impl.ts`; both rows below RESOLVED this PR (branch `claude/g-gate-j-pr-merge-proof`).

| id | title | summary | acceptance | source |
|----|-------|---------|------------|--------|
| G-GATE-J | `gh pr merge`-class delivery proof measured the WRONG ground-truth object (local push range, not the PR diff) | `protectedMutationClassForPromptGate` and `isProvenNonOntologyDelivery` both proved a `gh pr` subcommand's change-set via `allPushRangePathsNonOntology` (`git diff origin/<branch>...HEAD`) — the local PUSH RANGE. For `merge/close/reopen/edit/ready/review` (subcommands that operate on an EXISTING PR), the change-set ground truth is the PR's OWN diff, not the push range: the range is EMPTY (and thus conservatively `false`, over-blocking) whenever the calling session's HEAD is already synced with its upstream — exactly the case a `gh pr merge` run from a synced `main` hits, confirmed empirically (`allPushRangePathsNonOntology` returned `false` for both the marketplace and home repos on a synced main, `git diff --name-only origin/main...HEAD` producing zero lines even though `origin/main == HEAD`). So the A2 grant lane (`hasLiveDeliveryGrantForGate`) was unreachable for ANY `gh pr merge`, regardless of the PR's actual content — a live, correctly-minted `pm_authorize_delivery` grant could never clear the merge. | `gh pr merge/close/reopen/edit/ready/review` prove their change-set via `gh pr diff --name-only` (the PR's own diff), not the local push range; `gh pr create` (no existing PR yet) is unchanged; regression tests cover the empty-synced-main case, an ontology-poisoned PR diff (stays blocked), a `gh` failure/network-error (fail-closed), and `create` staying on the old push-range path | live smoke 2026-07-12 (user-reported); Lead mechanism verification citing `hooks/gates/prompt-dtc-enforcement-gate.impl.ts` lines ~240-246 (`protectedMutationClassForPromptGate`) + ~653-699 (`isProvenNonOntologyDelivery`); **RESOLVED 2026-07-12: this PR (pm 7.47.0) — new shared helper `ghPrChangeSetNonOntology`/`resolveGhPrDiffPaths` (execFileSync argv array, no shell, so a parsed selector token cannot open a second hook-internal injection surface) used at BOTH call sites so they cannot drift; `create` keeps the original `allPushRangePathsNonOntology` semantics byte-identical; fail-closed on any `gh` throw/empty output; regression coverage in `tests/hooks/prompt-dtc-enforcement-gate-gh-pr-diff-ground-truth.test.ts` (cases a-e) plus updated fake-`gh` PATH shims in the existing `-authorized-delivery(-grant)` suites** |
| G-DOCS-K | docs-class audit-after carve-out for the A2 delivery-proof predicate (USER directive 2026-07-12) | USER directive 2026-07-12 (audit-after ruling): a Markdown doc path is audited AFTER the fact (git history + PR review), never a live ontology source, so it should not force a delivery action to the `pull-request`/`commit`/`release` BLOCKING floor merely for containing an `ontology/`-segment-shaped substring (e.g. a doc literally named `ontology-notes.md`, or living under `projects/<p>/ontology/`). None of the 21 concrete home-PR paths surveyed (PRs #926/#927/#928) actually hit this collision — R1 alone would have unblocked them — so this carve is prospective/defense-in-depth, not a fix for an observed false-positive. | a `.md` path is treated as non-ontology by the A2 write-set proof (`isNonOntologyPath`) UNLESS it is an ontology SCHEMA surface (`schemas/ontology/`) or lives under the live `.palantir-mini/` ontology-state dir; `scopedBlockingFileReason` (the edit-time/advisory surface) is BYTE-IDENTICAL — the carve applies ONLY to the delivery-proof predicate; a delivery whose change-set contains a TRUE (non-docs-class) ontology surface keeps its blocking floor even with a live grant (R3, by-design, not a bug) | USER directive 2026-07-12 (audit-after ruling, docs-class carve-out); **RESOLVED 2026-07-12: this PR (pm 7.47.0) — new predicate `isDocsClassAuditAfterPath` wired into `isNonOntologyPath` ONLY, checked after the two hard floors (`.palantir-mini/`, `schemas/ontology/`) and before `pathIsProjectOntologyClass`; regression coverage in `tests/hooks/prompt-dtc-enforcement-gate-gh-pr-diff-ground-truth.test.ts` (cases d1-d5), including the R3 by-design assertion (an ontology-path-poisoned PR diff still denies even with a live grant) and a direct assertion that `scopedBlockingFileReason` is unaffected** |
