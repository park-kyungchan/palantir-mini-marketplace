// Scaffold marker (ledger row P320). No semantic-core, altitude, governance,
// control-plane, memory, lineage, migration, or adapter logic exists yet —
// this file exists solely so `tsc --noEmit` has an input to compile,
// proving the package is buildable at scaffold time. It is the only file
// under src/** at this stage; every sibling directory named in
// docs/architecture.md and BROWSE.md is intentionally empty until its
// owning wave (P330 onward, see BROWSE.md for the per-directory wave map).
export {};
