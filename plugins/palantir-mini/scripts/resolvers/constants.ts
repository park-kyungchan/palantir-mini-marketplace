// Minimal shared constants for the Codex resolver set.
// Full gstack constants (AI_SLOP_BLACKLIST etc.) were design-resolver specific
// and dropped with the design/* port exclusion.

export const PLUGIN_ROOT_VAR = "${PALANTIR_MINI_PLUGIN_ROOT}";
